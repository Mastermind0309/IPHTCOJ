#include <stdio.h>
#include<stdlib.h>
#define min(a,b) a<b?a:b
int n,m, MIN=1e9+7,x;
int main()
{
    scanf("%d %d", &n, &m);
    for(int i=0;i<n;i++) for(int j=0;j<m;j++) scanf("%d",&x), MIN= min(MIN,x);
    printf("%d\n", MIN);
}
